git status
git add .
git commit -m "Implement authentication and role-based access"
git push

Antigravity Prompt Complete
        ↓
git status
        ↓
git add .
        ↓
git commit -m "Meaningful message"
        ↓
git push
        ↓
Next Prompt

| Prompt               | Commit Message                                                     |
| -------------------- | ------------------------------------------------------------------ |
| Project Foundation   | `git commit -m "Setup project foundation with Next.js and Prisma"` |
| Database Schema      | `git commit -m "Add complete Prisma database schema"`              |
| Authentication       | `git commit -m "Implement authentication and role-based access"`   |
| Landing Page         | `git commit -m "Create premium landing page UI"`                   |
| Student Dashboard    | `git commit -m "Build student dashboard interface"`                |
| Instructor Dashboard | `git commit -m "Add instructor dashboard"`                         |
| Course Module        | `git commit -m "Implement course management system"`               |
| AI Tutor             | `git commit -m "Build AI personal tutor module"`                   |
| Mock Tests           | `git commit -m "Add mock test system"`                             |
| Notes System         | `git commit -m "Implement notes and PDF management"`               |
| Notifications        | `git commit -m "Add notification system"`                          |
| Admin Dashboard      | `git commit -m "Create admin dashboard"`                           |
| Final Optimization   | `git commit -m "Optimize application performance and UI"`     
     |

| Step | Command                   | Kya karta hai?                                                             | Kab use karna hai?                           |
| ---- | ------------------------- | -------------------------------------------------------------------------- | -------------------------------------------- |
| 1    | `git status`              | Batata hai kaunsi files change hui hain, nayi bani hain ya delete hui hain | Har prompt complete hone ke baad sabse pehle |
| 2    | `git add .`               | Saare changes ko Git tracking ke liye stage karta hai                      | Jab tum changes save karna chahte ho         |
| 3    | `git commit -m "message"` | Project ka snapshot/save point banata hai                                  | `git add` ke baad                            |
| 4    | `git push`                | Local changes ko GitHub par upload karta hai                               | Commit ke baad                               |
